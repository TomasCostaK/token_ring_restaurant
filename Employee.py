# coding: utf-8

import time
import pickle
import socket
import logging
# import argparse
import threading
import Node

logging.basicConfig(level=logging.DEBUG,
                    format='%(asctime)s %(name)-12s %(levelname)-8s %(message)s',
                    datefmt='%m-%d %H:%M:%S')

class Employee(Node):
	def __init__(self)
		super().__init__(self, 3, address, timeout=3)
		self.queueIn = queue.Queue


def main(port, ring, timeout):


if __name__ == '__main__':
