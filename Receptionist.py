# coding: utf-8

import time
import pickle
import socket
import logging
# import argparse
import threading
import Node

logging.basicConfig(level=logging.DEBUG,
                    format='%(asctime)s %(name)-12s %(levelname)-8s %(message)s',
                    datefmt='%m-%d %H:%M:%S')

class Receptionist(Node):
	def __init__(self)
		super().__init__(self, 1, address, root_id, root_address, timeout=3)
		self.queueIn = queue.Queue
		self.queueOut = queue.Queue
		self.logger = logging.getLogger("Rececionista {}".format(self.id))


	def receiveRequest(self,args): #if method == FOOD_ORDER


	def askCook(self,args): 


	def send(self, address, o):


def main(port, ring, timeout):


if __name__ == '__main__':
